local _S={
    keypress      =type(keypress)=="function"           and keypress           or false,
    mouse1click   =type(mouse1click)=="function"         and mouse1click         or false,
    fireprox      =type(fireproximityprompt)=="function" and fireproximityprompt or false,
    fireclick     =type(fireclickdetector)=="function"   and fireclickdetector   or false,
    writefile     =type(writefile)=="function"           and writefile           or false,
    readfile      =type(readfile)=="function"            and readfile            or false,
}
local function sCall(fn,...) if fn then return pcall(fn,...) end return false end

if not task then
    task={
        wait =function(t) return wait(t or 0) end,
        spawn=function(f,...) return spawn(f,...) end,
        delay=function(t,f) return delay(t,f) end,
        defer=function(f) return spawn(f) end,
    }
end

local Players         = game:GetService("Players")
local RunService      = game:GetService("RunService")
local UIS             = game:GetService("UserInputService")
local TweenService    = game:GetService("TweenService")
local HttpService     = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local ContextAction   = game:GetService("ContextActionService")
local Debris          = game:GetService("Debris")
local Camera          = workspace.CurrentCamera
local LP              = Players.LocalPlayer
local Mouse           = LP:GetMouse()
local IsMobile        = UIS.TouchEnabled and not UIS.KeyboardEnabled

local VP=Camera.ViewportSize
local SW,SH=VP.X,VP.Y
local ScaleX=math.clamp(SW/1280,0.52,1.5)
local ScaleY=math.clamp(SH/720, 0.52,1.5)
local Scale =math.min(ScaleX,ScaleY)
local function S(n) return math.floor(n*Scale) end

local WIN_W =S(IsMobile and 340 or 320)
local WIN_H =S(IsMobile and 555 or 520)
local CARD_H=S(IsMobile and 60  or 52)
local SLD_H =S(IsMobile and 70  or 62)
local FONT_L=S(13) local FONT_M=S(11) local FONT_S=S(10) local FONT_XS=S(9)

Camera:GetPropertyChangedSignal("ViewportSize"):Connect(function()
    local vp=Camera.ViewportSize SW=vp.X SH=vp.Y
    ScaleX=math.clamp(SW/1280,0.52,1.5) ScaleY=math.clamp(SH/720,0.52,1.5) Scale=math.min(ScaleX,ScaleY)
end)

local CFG_FILE="PelmenPVP_b001_cfg.json"
local DEFAULT={
    Speed={On=false,Value=32}, InfJump={On=false,Power=50},
    Fling={On=false,Power=300,Range=25}, AntiAC={On=false},
    AntiRag={On=false}, ShiftLock={On=false}, Spam={On=false,Delay=7},
    ServLag={On=false,Threads=50}, Fly={On=false,Speed=5},
    FastCollect={On=false,Rate=5},
}
local function DeepCopy(t) local c={} for k,v in pairs(t) do c[k]=type(v)=="table" and DeepCopy(v) or v end return c end
local function MergeCfg(base,saved)
    if type(saved)~="table" then return DeepCopy(base) end
    local m=DeepCopy(base)
    for k,v in pairs(saved) do if type(v)=="table" and type(m[k])=="table" then
        for kk,vv in pairs(v) do if m[k][kk]~=nil then m[k][kk]=vv end end
    end end return m
end
local function LoadCfg()
    if _S.readfile then
        local ok,raw=sCall(_S.readfile,CFG_FILE)
        if ok and raw and #raw>2 then
            local ok2,p=pcall(HttpService.JSONDecode,HttpService,raw)
            if ok2 and p then return MergeCfg(DEFAULT,p) end
        end
    end
    return DeepCopy(DEFAULT)
end
local function SaveCfg(cfg)
    if _S.writefile then
        local ok,json=pcall(HttpService.JSONEncode,HttpService,cfg)
        if ok then sCall(_S.writefile,CFG_FILE,json) end
    end
end
local Cfg=LoadCfg()
local _saveT=0
local function ScheduleSave() _saveT=tick() end

local function Char() return LP.Character end
local function Root() local c=Char() return c and c:FindFirstChild("HumanoidRootPart") end
local function Hum()  local c=Char() return c and c:FindFirstChildOfClass("Humanoid") end

local T={
    BG=Color3.fromRGB(8,8,14),       Panel=Color3.fromRGB(14,14,22),
    Card=Color3.fromRGB(20,20,32),    CardHov=Color3.fromRGB(28,28,42),
    Acc=Color3.fromRGB(255,65,0),     Acc2=Color3.fromRGB(255,140,30),
    Blue=Color3.fromRGB(40,130,255),  Red=Color3.fromRGB(220,50,50),
    Green=Color3.fromRGB(40,210,90),  Purple=Color3.fromRGB(140,80,255),
    Cyan=Color3.fromRGB(30,210,210),  Yellow=Color3.fromRGB(255,210,40),
    Sky=Color3.fromRGB(80,185,255),   Lime=Color3.fromRGB(110,255,90),
    Off=Color3.fromRGB(36,36,52),     Text=Color3.fromRGB(215,215,228),
    Sub=Color3.fromRGB(95,95,120),    White=Color3.fromRGB(255,255,255),
    DPad=Color3.fromRGB(30,120,220),
}

local function Tw(o,p,t,s)
    TweenService:Create(o,TweenInfo.new(t or 0.18,s or Enum.EasingStyle.Quart,Enum.EasingDirection.Out),p):Play()
end
local function uiCorner(p,r) local c=Instance.new("UICorner") c.CornerRadius=UDim.new(0,r or 8) c.Parent=p return c end
local function uiStroke(p,c,th) local s=Instance.new("UIStroke") s.Color=c s.Thickness=th or 1.2 s.Parent=p return s end
local function uiPad(p,l,r,t,b)
    local u=Instance.new("UIPadding")
    u.PaddingLeft=UDim.new(0,l or 0) u.PaddingRight=UDim.new(0,r or 0)
    u.PaddingTop=UDim.new(0,t or 0)  u.PaddingBottom=UDim.new(0,b or 0) u.Parent=p
end

if game.CoreGui:FindFirstChild("PelmenB001") then game.CoreGui.PelmenB001:Destroy() end

local GUI=Instance.new("ScreenGui")
GUI.Name="PelmenB001" GUI.ResetOnSpawn=false
GUI.ZIndexBehavior=Enum.ZIndexBehavior.Sibling
GUI.DisplayOrder=1000 GUI.IgnoreGuiInset=true
local _ok=pcall(function() GUI.Parent=game.CoreGui end)
if not _ok then GUI.Parent=LP.PlayerGui end

local LoadFrame=Instance.new("Frame",GUI)
LoadFrame.Size=UDim2.new(1,0,1,0) LoadFrame.BackgroundColor3=T.BG LoadFrame.BorderSizePixel=0 LoadFrame.ZIndex=9999
local LoadGrad=Instance.new("UIGradient",LoadFrame)
LoadGrad.Color=ColorSequence.new{ColorSequenceKeypoint.new(0,Color3.fromRGB(6,6,10)),ColorSequenceKeypoint.new(1,Color3.fromRGB(14,10,22))}
LoadGrad.Rotation=135

local LoadCenter=Instance.new("Frame",LoadFrame)
LoadCenter.AnchorPoint=Vector2.new(0.5,0.5) LoadCenter.Position=UDim2.new(0.5,0,0.5,0)
LoadCenter.Size=UDim2.new(0,S(320),0,S(290)) LoadCenter.BackgroundTransparency=1 LoadCenter.ZIndex=10000

local LogoGlow=Instance.new("ImageLabel",LoadCenter)
LogoGlow.Image="rbxassetid://5028857084"
LogoGlow.Size=UDim2.new(0,S(160),0,S(160)) LogoGlow.AnchorPoint=Vector2.new(0.5,0)
LogoGlow.Position=UDim2.new(0.5,0,0,-S(20)) LogoGlow.BackgroundTransparency=1 LogoGlow.ZIndex=9999
LogoGlow.ImageColor3=T.Acc LogoGlow.ImageTransparency=0.7

local LogoImg=Instance.new("ImageLabel",LoadCenter)
LogoImg.Image="rbxassetid://101926931672087"
LogoImg.Size=UDim2.new(0,S(120),0,S(120)) LogoImg.AnchorPoint=Vector2.new(0.5,0)
LogoImg.Position=UDim2.new(0.5,0,0,0) LogoImg.BackgroundTransparency=1 LogoImg.ZIndex=10001 LogoImg.ImageTransparency=1
uiCorner(LogoImg,S(22))

local LogoFallback=Instance.new("TextLabel",LoadCenter)
LogoFallback.Text="🥟" LogoFallback.Size=UDim2.new(0,S(120),0,S(120)) LogoFallback.AnchorPoint=Vector2.new(0.5,0)
LogoFallback.Position=UDim2.new(0.5,0,0,0) LogoFallback.BackgroundTransparency=1 LogoFallback.TextScaled=true LogoFallback.ZIndex=10000
LogoImg:GetPropertyChangedSignal("IsLoaded"):Connect(function() if LogoImg.IsLoaded then LogoFallback.Visible=false end end)

local LoadTitle=Instance.new("TextLabel",LoadCenter)
LoadTitle.Text="PELMEN PVP" LoadTitle.Size=UDim2.new(1,0,0,S(36)) LoadTitle.Position=UDim2.new(0,0,0,S(132))
LoadTitle.BackgroundTransparency=1 LoadTitle.ZIndex=10001 LoadTitle.TextColor3=T.White
LoadTitle.Font=Enum.Font.GothamBold LoadTitle.TextSize=S(28) LoadTitle.TextTransparency=1

local LoadVer=Instance.new("TextLabel",LoadCenter)
LoadVer.Text="Beta 0.0.1  ·  "..(IsMobile and "📱 Mobile" or "🖥️ PC").."  ·  Safe"
LoadVer.Size=UDim2.new(1,0,0,S(18)) LoadVer.Position=UDim2.new(0,0,0,S(170))
LoadVer.BackgroundTransparency=1 LoadVer.ZIndex=10001 LoadVer.TextColor3=T.Sub
LoadVer.Font=Enum.Font.Gotham LoadVer.TextSize=S(11) LoadVer.TextTransparency=1

local LoadStatus=Instance.new("TextLabel",LoadCenter)
LoadStatus.Text="Инициализация..."
LoadStatus.Size=UDim2.new(1,0,0,S(16)) LoadStatus.Position=UDim2.new(0,0,0,S(200))
LoadStatus.BackgroundTransparency=1 LoadStatus.ZIndex=10001 LoadStatus.TextColor3=T.Acc
LoadStatus.Font=Enum.Font.Gotham LoadStatus.TextSize=S(10) LoadStatus.TextTransparency=1

local BarTrack=Instance.new("Frame",LoadCenter)
BarTrack.Size=UDim2.new(1,0,0,S(4)) BarTrack.Position=UDim2.new(0,0,0,S(228))
BarTrack.BackgroundColor3=T.Off BarTrack.BorderSizePixel=0 BarTrack.ZIndex=10001
uiCorner(BarTrack,S(3))
local BarFill=Instance.new("Frame",BarTrack)
BarFill.Size=UDim2.new(0,0,1,0) BarFill.BackgroundColor3=T.Acc BarFill.BorderSizePixel=0 BarFill.ZIndex=10002
uiCorner(BarFill,S(3))
local BarGrad=Instance.new("UIGradient",BarFill)
BarGrad.Color=ColorSequence.new{ColorSequenceKeypoint.new(0,T.Acc),ColorSequenceKeypoint.new(1,T.Acc2)}

local LoadSub=Instance.new("TextLabel",LoadFrame)
LoadSub.Text="Delta · Arceus X · Fluxus · Synapse  |  Beta 0.0.1"
LoadSub.Size=UDim2.new(1,0,0,S(20)) LoadSub.Position=UDim2.new(0,0,1,-S(30))
LoadSub.BackgroundTransparency=1 LoadSub.ZIndex=10001 LoadSub.TextColor3=T.Sub
LoadSub.Font=Enum.Font.Gotham LoadSub.TextSize=S(10) LoadSub.TextTransparency=1

local function animLoad(pct,status)
    LoadStatus.Text=status
    Tw(BarFill,{Size=UDim2.new(pct,0,1,0)},0.4,Enum.EasingStyle.Quart)
end

task.spawn(function()
    Tw(LogoImg,   {ImageTransparency=0},0.6,Enum.EasingStyle.Quart)
    Tw(LoadTitle, {TextTransparency=0}, 0.7,Enum.EasingStyle.Quart)
    Tw(LoadVer,   {TextTransparency=0}, 0.8,Enum.EasingStyle.Quart)
    Tw(LoadStatus,{TextTransparency=0}, 0.5,Enum.EasingStyle.Quart)
    Tw(LoadSub,   {TextTransparency=0}, 0.9,Enum.EasingStyle.Quart)
    task.wait(0.3)
    task.spawn(function()
        while LoadFrame.Parent do
            Tw(LogoGlow,{ImageTransparency=0.45},0.85,Enum.EasingStyle.Sine)
            task.wait(0.9)
            Tw(LogoGlow,{ImageTransparency=0.78},0.85,Enum.EasingStyle.Sine)
            task.wait(0.9)
        end
    end)
    animLoad(0.15,"Загрузка сервисов...")    task.wait(0.3)
    animLoad(0.35,"Инициализация UI...")      task.wait(0.28)
    animLoad(0.52,"Загрузка конфига...")      task.wait(0.28)
    animLoad(0.68,"Fly система...")           task.wait(0.22)
    animLoad(0.82,"Anti-Cheat защита...")     task.wait(0.22)
    animLoad(0.94,"Fast Collect...")          task.wait(0.18)
    animLoad(1.00,"Готово ✓")                task.wait(0.55)
    Tw(LoadFrame, {BackgroundTransparency=1},0.5,Enum.EasingStyle.Quart)
    Tw(LogoImg,   {ImageTransparency=1},     0.4)
    Tw(LoadTitle, {TextTransparency=1},      0.4)
    Tw(LoadVer,   {TextTransparency=1},      0.4)
    Tw(LoadStatus,{TextTransparency=1},      0.3)
    Tw(LoadSub,   {TextTransparency=1},      0.3)
    task.wait(0.55)
    if LoadFrame and LoadFrame.Parent then LoadFrame:Destroy() end
end)

local Win=Instance.new("Frame",GUI)
Win.Name="MainWin"
Win.Size=UDim2.new(0,WIN_W,0,WIN_H)
Win.Position=UDim2.new(0,S(18),0.5,-WIN_H/2)
Win.BackgroundColor3=T.BG Win.BorderSizePixel=0 Win.ClipsDescendants=true
uiCorner(Win,S(14)) uiStroke(Win,T.Acc,1.4)

local TLine=Instance.new("Frame",Win)
TLine.Size=UDim2.new(1,0,0,S(3)) TLine.BackgroundColor3=T.Acc TLine.BorderSizePixel=0 TLine.ZIndex=10
local TG=Instance.new("UIGradient",TLine)
TG.Color=ColorSequence.new{ColorSequenceKeypoint.new(0,T.Acc),ColorSequenceKeypoint.new(0.5,T.Acc2),ColorSequenceKeypoint.new(1,T.Acc)}

local HEAD_H=S(56)
local Head=Instance.new("Frame",Win)
Head.Size=UDim2.new(1,0,0,HEAD_H) Head.Position=UDim2.new(0,0,0,S(3))
Head.BackgroundColor3=T.Panel Head.BorderSizePixel=0 Head.ZIndex=5

local LOGO_SZ=S(40)
local LogoBg=Instance.new("Frame",Head)
LogoBg.Size=UDim2.new(0,LOGO_SZ,0,LOGO_SZ) LogoBg.Position=UDim2.new(0,S(10),0.5,-LOGO_SZ/2)
LogoBg.BackgroundColor3=T.Acc LogoBg.BorderSizePixel=0 LogoBg.ZIndex=6 uiCorner(LogoBg,S(10))
local HeaderLogo=Instance.new("ImageLabel",LogoBg)
HeaderLogo.Image="rbxassetid://101926931672087"
HeaderLogo.Size=UDim2.new(1,0,1,0) HeaderLogo.BackgroundTransparency=1 HeaderLogo.ZIndex=7 uiCorner(HeaderLogo,S(10))
local HFallback=Instance.new("TextLabel",LogoBg)
HFallback.Text="🥟" HFallback.Size=UDim2.new(1,0,1,0) HFallback.BackgroundTransparency=1 HFallback.TextScaled=true HFallback.ZIndex=6
HeaderLogo:GetPropertyChangedSignal("IsLoaded"):Connect(function() if HeaderLogo.IsLoaded then HFallback.Visible=false end end)

local TitleL=Instance.new("TextLabel",Head)
TitleL.Text="PELMEN PVP" TitleL.Size=UDim2.new(0,S(160),0,S(20))
TitleL.Position=UDim2.new(0,S(58),0,S(7)) TitleL.BackgroundTransparency=1
TitleL.TextColor3=T.White TitleL.Font=Enum.Font.GothamBold TitleL.TextSize=S(15)
TitleL.TextXAlignment=Enum.TextXAlignment.Left TitleL.ZIndex=6

local VerL=Instance.new("TextLabel",Head)
VerL.Text="Beta 0.0.1 · "..(IsMobile and "📱 Mobile" or "🖥️ PC")
VerL.Size=UDim2.new(0,S(200),0,S(14)) VerL.Position=UDim2.new(0,S(58),0,S(29))
VerL.BackgroundTransparency=1 VerL.TextColor3=T.Sub
VerL.Font=Enum.Font.Gotham VerL.TextSize=S(10)
VerL.TextXAlignment=Enum.TextXAlignment.Left VerL.ZIndex=6

local MinBtn=Instance.new("TextButton",Head)
MinBtn.Text="−" MinBtn.Size=UDim2.new(0,S(28),0,S(28))
MinBtn.Position=UDim2.new(1,-S(40),0.5,-S(14))
MinBtn.BackgroundColor3=T.Card MinBtn.TextColor3=T.Sub
MinBtn.Font=Enum.Font.GothamBold MinBtn.TextSize=S(18)
MinBtn.BorderSizePixel=0 MinBtn.AutoButtonColor=false MinBtn.ZIndex=6
uiCorner(MinBtn,S(8))

local TAB_H=S(30)
local TabBar=Instance.new("Frame",Win)
TabBar.Size=UDim2.new(1,-S(24),0,TAB_H) TabBar.Position=UDim2.new(0,S(12),0,HEAD_H+S(6))
TabBar.BackgroundColor3=T.Panel TabBar.BorderSizePixel=0 TabBar.ZIndex=5 uiCorner(TabBar,S(8))
local TBL=Instance.new("UIListLayout",TabBar)
TBL.FillDirection=Enum.FillDirection.Horizontal TBL.SortOrder=Enum.SortOrder.LayoutOrder TBL.Padding=UDim.new(0,S(3))
uiPad(TabBar,S(4),S(4),S(4),S(4))

local SCROLL_TOP=HEAD_H+TAB_H+S(14)
local Scroll=Instance.new("ScrollingFrame",Win)
Scroll.Size=UDim2.new(1,-S(18),1,-SCROLL_TOP-S(6)) Scroll.Position=UDim2.new(0,S(9),0,SCROLL_TOP)
Scroll.BackgroundTransparency=1 Scroll.BorderSizePixel=0
Scroll.ScrollBarThickness=S(2) Scroll.ScrollBarImageColor3=T.Acc
Scroll.CanvasSize=UDim2.new(0,0,0,0) Scroll.AutomaticCanvasSize=Enum.AutomaticSize.Y
local SLL=Instance.new("UIListLayout",Scroll)
SLL.Padding=UDim.new(0,S(7)) SLL.SortOrder=Enum.SortOrder.LayoutOrder
uiPad(Scroll,0,0,S(5),S(5))

local nStack=0
local function Notify(title,msg,col,dur)
    nStack=nStack+1
    dur=dur or 3 col=col or T.Acc
    local slot=nStack local NW=S(272) local NH=S(58)
    local N=Instance.new("Frame",GUI)
    N.Size=UDim2.new(0,NW,0,NH) N.Position=UDim2.new(1,10,1,-(NH+S(8))*slot-S(10))
    N.BackgroundColor3=T.Panel N.BorderSizePixel=0 N.ZIndex=200
    uiCorner(N,S(10)) uiStroke(N,col,1)
    local Bar=Instance.new("Frame",N)
    Bar.Size=UDim2.new(0,S(3),0.65,0) Bar.Position=UDim2.new(0,0,0.175,0)
    Bar.BackgroundColor3=col Bar.BorderSizePixel=0 Bar.ZIndex=201 uiCorner(Bar,S(2))
    local TL=Instance.new("TextLabel",N)
    TL.Text=title TL.Size=UDim2.new(1,-S(14),0,S(22)) TL.Position=UDim2.new(0,S(10),0,S(7))
    TL.BackgroundTransparency=1 TL.TextColor3=T.White TL.Font=Enum.Font.GothamBold TL.TextSize=S(12)
    TL.TextXAlignment=Enum.TextXAlignment.Left TL.ZIndex=201
    local ML=Instance.new("TextLabel",N)
    ML.Text=msg ML.Size=UDim2.new(1,-S(14),0,S(16)) ML.Position=UDim2.new(0,S(10),0,S(32))
    ML.BackgroundTransparency=1 ML.TextColor3=T.Sub ML.Font=Enum.Font.Gotham ML.TextSize=S(10)
    ML.TextXAlignment=Enum.TextXAlignment.Left ML.ZIndex=201
    Tw(N,{Position=UDim2.new(1,-NW-S(12),1,-(NH+S(8))*slot-S(10))},0.35,Enum.EasingStyle.Back)
    task.delay(dur,function()
        Tw(N,{Position=UDim2.new(1,10,1,-(NH+S(8))*slot-S(10))},0.25)
        task.wait(0.28) if N and N.Parent then N:Destroy() end nStack=math.max(0,nStack-1)
    end)
end

local function Section(txt,color)
    color=color or T.Sub
    local F=Instance.new("Frame",Scroll) F.Size=UDim2.new(1,0,0,S(20)) F.BackgroundTransparency=1
    local B=Instance.new("Frame",F) B.Size=UDim2.new(0,S(3),0.7,0) B.Position=UDim2.new(0,0,0.15,0)
    B.BackgroundColor3=color B.BorderSizePixel=0 uiCorner(B,S(2))
    local L=Instance.new("TextLabel",F) L.Text=txt:upper() L.Size=UDim2.new(1,-S(10),1,0) L.Position=UDim2.new(0,S(8),0,0)
    L.BackgroundTransparency=1 L.TextColor3=color L.Font=Enum.Font.GothamBold L.TextSize=FONT_XS L.TextXAlignment=Enum.TextXAlignment.Left
end

local function Toggle(label,icon,desc,acc,getV,setV,onChange)
    acc=acc or T.Acc
    local Card=Instance.new("Frame",Scroll) Card.Size=UDim2.new(1,0,0,CARD_H) Card.BackgroundColor3=T.Card Card.BorderSizePixel=0
    uiCorner(Card,S(10)) local stk=uiStroke(Card,T.Off,1)
    local IB_SZ=S(34) local IB=Instance.new("Frame",Card)
    IB.Size=UDim2.new(0,IB_SZ,0,IB_SZ) IB.Position=UDim2.new(0,S(10),0.5,-IB_SZ/2)
    IB.BackgroundColor3=getV() and acc or T.Panel IB.BorderSizePixel=0 uiCorner(IB,S(9))
    local IL=Instance.new("TextLabel",IB) IL.Text=icon IL.Size=UDim2.new(1,0,1,0) IL.BackgroundTransparency=1 IL.TextScaled=true IL.ZIndex=2
    local NL=Instance.new("TextLabel",Card) NL.Text=label NL.Size=UDim2.new(0,S(140),0,S(18)) NL.Position=UDim2.new(0,S(52),0,S(7))
    NL.BackgroundTransparency=1 NL.TextColor3=getV() and T.White or T.Text NL.Font=Enum.Font.GothamBold NL.TextSize=FONT_L NL.TextXAlignment=Enum.TextXAlignment.Left
    local DL=Instance.new("TextLabel",Card) DL.Text=desc DL.Size=UDim2.new(0,S(155),0,S(13)) DL.Position=UDim2.new(0,S(52),0,S(27))
    DL.BackgroundTransparency=1 DL.TextColor3=T.Sub DL.Font=Enum.Font.Gotham DL.TextSize=FONT_S DL.TextXAlignment=Enum.TextXAlignment.Left
    local PW,PH=S(44),S(22) local Pill=Instance.new("Frame",Card)
    Pill.Size=UDim2.new(0,PW,0,PH) Pill.Position=UDim2.new(1,-PW-S(10),0.5,-PH/2)
    Pill.BackgroundColor3=getV() and acc or T.Off Pill.BorderSizePixel=0 uiCorner(Pill,S(11))
    local KSZ=S(16) local Knob=Instance.new("Frame",Pill)
    Knob.Size=UDim2.new(0,KSZ,0,KSZ)
    Knob.Position=getV() and UDim2.new(1,-KSZ-S(3),0.5,-KSZ/2) or UDim2.new(0,S(3),0.5,-KSZ/2)
    Knob.BackgroundColor3=T.White Knob.BorderSizePixel=0 uiCorner(Knob,S(8))
    local Dot=Instance.new("Frame",Card) Dot.Size=UDim2.new(0,S(6),0,S(6)) Dot.Position=UDim2.new(1,-S(13),0,S(8))
    Dot.BackgroundColor3=getV() and T.Green or T.Sub Dot.BorderSizePixel=0 uiCorner(Dot,S(3))
    local function Refresh(s)
        Tw(Pill,{BackgroundColor3=s and acc or T.Off})
        Tw(Knob,{Position=s and UDim2.new(1,-KSZ-S(3),0.5,-KSZ/2) or UDim2.new(0,S(3),0.5,-KSZ/2),BackgroundColor3=s and T.White or Color3.fromRGB(155,155,175)})
        Tw(IB,{BackgroundColor3=s and acc or T.Panel}) Tw(stk,{Color=s and acc or T.Off}) Tw(Dot,{BackgroundColor3=s and T.Green or T.Sub})
        NL.TextColor3=s and T.White or T.Text
    end
    Card.InputBegan:Connect(function(i)
        if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then
            setV(not getV()) Refresh(getV()) ScheduleSave() if onChange then onChange(getV()) end
        end
    end)
    return Card,Refresh
end

local function Slider(label,icon,acc,minV,maxV,getV,setV,suffix,onChange)
    suffix=suffix or "" acc=acc or T.Acc
    local Card=Instance.new("Frame",Scroll) Card.Size=UDim2.new(1,0,0,SLD_H) Card.BackgroundColor3=T.Card Card.BorderSizePixel=0 uiCorner(Card,S(10))
    local IL=Instance.new("TextLabel",Card) IL.Text=icon IL.Size=UDim2.new(0,S(26),0,S(26)) IL.Position=UDim2.new(0,S(10),0,S(10)) IL.BackgroundTransparency=1 IL.TextScaled=true
    local NL=Instance.new("TextLabel",Card) NL.Text=label NL.Size=UDim2.new(0,S(140),0,S(18)) NL.Position=UDim2.new(0,S(40),0,S(8))
    NL.BackgroundTransparency=1 NL.TextColor3=T.Text NL.Font=Enum.Font.GothamBold NL.TextSize=FONT_M NL.TextXAlignment=Enum.TextXAlignment.Left
    local VL=Instance.new("TextLabel",Card) VL.Text=getV()..suffix VL.Size=UDim2.new(0,S(72),0,S(18)) VL.Position=UDim2.new(1,-S(78),0,S(8))
    VL.BackgroundTransparency=1 VL.TextColor3=acc VL.Font=Enum.Font.GothamBold VL.TextSize=FONT_M VL.TextXAlignment=Enum.TextXAlignment.Right
    local Track=Instance.new("Frame",Card) Track.Size=UDim2.new(1,-S(20),0,S(6)) Track.Position=UDim2.new(0,S(10),0,S(42))
    Track.BackgroundColor3=T.Panel Track.BorderSizePixel=0 uiCorner(Track,S(3))
    local pct=(getV()-minV)/(maxV-minV)
    local Fill=Instance.new("Frame",Track) Fill.Size=UDim2.new(pct,0,1,0) Fill.BackgroundColor3=acc Fill.BorderSizePixel=0 uiCorner(Fill,S(3))
    local Handle=Instance.new("Frame",Track) Handle.Size=UDim2.new(0,S(14),0,S(14)) Handle.AnchorPoint=Vector2.new(0.5,0.5)
    Handle.Position=UDim2.new(pct,0,0.5,0) Handle.BackgroundColor3=T.White Handle.BorderSizePixel=0 Handle.ZIndex=3 uiCorner(Handle,S(7))
    local dragging=false
    local function SetVal(x)
        local a=Track.AbsolutePosition.X local w=Track.AbsoluteSize.X
        local p=math.clamp((x-a)/w,0,1) local v=math.floor(minV+p*(maxV-minV))
        setV(v) VL.Text=v..suffix Tw(Fill,{Size=UDim2.new(p,0,1,0)}) Tw(Handle,{Position=UDim2.new(p,0,0.5,0)})
        ScheduleSave() if onChange then onChange(v) end
    end
    Track.InputBegan:Connect(function(i)
        if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then dragging=true SetVal(i.Position.X) end
    end)
    UIS.InputChanged:Connect(function(i)
        if dragging and (i.UserInputType==Enum.UserInputType.MouseMovement or i.UserInputType==Enum.UserInputType.Touch) then SetVal(i.Position.X) end
    end)
    UIS.InputEnded:Connect(function(i)
        if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then dragging=false end
    end)
end

local function ActionBtn(label,icon,color,desc,onClick)
    local Card=Instance.new("Frame",Scroll) Card.Size=UDim2.new(1,0,0,CARD_H) Card.BackgroundColor3=T.Card Card.BorderSizePixel=0
    uiCorner(Card,S(10)) local stk=uiStroke(Card,T.Off,1)
    local IB_SZ=S(34) local IB=Instance.new("Frame",Card)
    IB.Size=UDim2.new(0,IB_SZ,0,IB_SZ) IB.Position=UDim2.new(0,S(10),0.5,-IB_SZ/2)
    IB.BackgroundColor3=T.Panel IB.BorderSizePixel=0 uiCorner(IB,S(9))
    local IL=Instance.new("TextLabel",IB) IL.Text=icon IL.Size=UDim2.new(1,0,1,0) IL.BackgroundTransparency=1 IL.TextScaled=true IL.ZIndex=2
    local NL=Instance.new("TextLabel",Card) NL.Text=label NL.Size=UDim2.new(0,S(140),0,S(18)) NL.Position=UDim2.new(0,S(52),0,S(7))
    NL.BackgroundTransparency=1 NL.TextColor3=T.Text NL.Font=Enum.Font.GothamBold NL.TextSize=FONT_L NL.TextXAlignment=Enum.TextXAlignment.Left
    local DL=Instance.new("TextLabel",Card) DL.Text=desc DL.Size=UDim2.new(0,S(145),0,S(13)) DL.Position=UDim2.new(0,S(52),0,S(27))
    DL.BackgroundTransparency=1 DL.TextColor3=T.Sub DL.Font=Enum.Font.Gotham DL.TextSize=FONT_S DL.TextXAlignment=Enum.TextXAlignment.Left
    local BW,BH=S(58),S(28) local RunBtn=Instance.new("TextButton",Card)
    RunBtn.Text="▶ Run" RunBtn.Size=UDim2.new(0,BW,0,BH) RunBtn.Position=UDim2.new(1,-BW-S(10),0.5,-BH/2)
    RunBtn.BackgroundColor3=color RunBtn.TextColor3=T.White RunBtn.Font=Enum.Font.GothamBold RunBtn.TextSize=FONT_M
    RunBtn.BorderSizePixel=0 RunBtn.AutoButtonColor=false uiCorner(RunBtn,S(8))
    local function flash()
        Tw(RunBtn,{BackgroundColor3=T.White},0.08) Tw(IB,{BackgroundColor3=color},0.08) Tw(stk,{Color=color})
        task.delay(0.18,function() Tw(RunBtn,{BackgroundColor3=color},0.18) Tw(IB,{BackgroundColor3=T.Panel},0.4) Tw(stk,{Color=T.Off},0.4) end)
        pcall(onClick)
    end
    RunBtn.MouseButton1Click:Connect(flash) RunBtn.TouchTap:Connect(flash)
    Card.InputBegan:Connect(function(i)
        if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then Tw(Card,{BackgroundColor3=T.CardHov},0.1) end
    end)
    Card.InputEnded:Connect(function(i)
        if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then Tw(Card,{BackgroundColor3=T.Card},0.1) end
    end)
end

local flyActive=false local flyBG=nil local flyBV=nil
local flySpd=0 local flyCtrl={f=0,b=0,l=0,r=0} local flyLastCtrl={f=0,b=0,l=0,r=0}
local mobFlyCtrl={f=false,b=false,l=false,r=false,u=false,d=false}

local function GetFlyMax() return Cfg.Fly.Speed*8 end

local function UpdateFlyCtrl()
    flyCtrl.f=((UIS:IsKeyDown(Enum.KeyCode.W) or UIS:IsKeyDown(Enum.KeyCode.Up))    or mobFlyCtrl.f) and 1  or 0
    flyCtrl.b=((UIS:IsKeyDown(Enum.KeyCode.S) or UIS:IsKeyDown(Enum.KeyCode.Down))  or mobFlyCtrl.b) and -1 or 0
    flyCtrl.l=((UIS:IsKeyDown(Enum.KeyCode.A) or UIS:IsKeyDown(Enum.KeyCode.Left))  or mobFlyCtrl.l) and -1 or 0
    flyCtrl.r=((UIS:IsKeyDown(Enum.KeyCode.D) or UIS:IsKeyDown(Enum.KeyCode.Right)) or mobFlyCtrl.r) and 1  or 0
end

local function StopFly()
    flyActive=false
    local char=Char() if char then
        local h=char:FindFirstChildOfClass("Humanoid") if h then
            for _,s in pairs(Enum.HumanoidStateType:GetEnumItems()) do pcall(function() h:SetStateEnabled(s,true) end) end
            pcall(function() h:ChangeState(Enum.HumanoidStateType.RunningNoPhysics) end)
            h.PlatformStand=false
        end
        local anim=char:FindFirstChild("Animate") if anim then anim.Disabled=false end
    end
    if flyBG and flyBG.Parent then flyBG:Destroy() end flyBG=nil
    if flyBV and flyBV.Parent then flyBV:Destroy() end flyBV=nil
    flySpd=0 flyCtrl={f=0,b=0,l=0,r=0} flyLastCtrl={f=0,b=0,l=0,r=0}
    mobFlyCtrl={f=false,b=false,l=false,r=false,u=false,d=false}
end

local function StartFly()
    if flyActive then StopFly() return end
    local char=Char() if not char then return end
    local h=char:FindFirstChildOfClass("Humanoid") if not h then return end
    local isR6=h.RigType==Enum.HumanoidRigType.R6
    local torso=isR6 and char:FindFirstChild("Torso") or char:FindFirstChild("UpperTorso")
    if not torso then return end
    flyActive=true
    for _,s in pairs(Enum.HumanoidStateType:GetEnumItems()) do pcall(function() h:SetStateEnabled(s,false) end) end
    pcall(function() h:ChangeState(Enum.HumanoidStateType.Swimming) end)
    h.PlatformStand=true
    local anim=char:FindFirstChild("Animate") if anim then anim.Disabled=true end
    for _,tr in pairs(h:GetPlayingAnimationTracks()) do pcall(function() tr:AdjustSpeed(0) end) end
    flyBG=Instance.new("BodyGyro",torso) flyBG.P=9e4 flyBG.MaxTorque=Vector3.new(9e9,9e9,9e9) flyBG.CFrame=torso.CFrame
    flyBV=Instance.new("BodyVelocity",torso) flyBV.Velocity=Vector3.new(0,0.1,0) flyBV.MaxForce=Vector3.new(9e9,9e9,9e9)
    task.spawn(function()
        flySpd=0
        while flyActive and char and h and h.Parent do
            RunService.RenderStepped:Wait() UpdateFlyCtrl()
            local maxS=GetFlyMax()
            local moving=(flyCtrl.l+flyCtrl.r)~=0 or (flyCtrl.f+flyCtrl.b)~=0
            if moving then flySpd=math.min(flySpd+0.5+flySpd/maxS,maxS) else flySpd=math.max(flySpd-1,0) end
            local camCF=Camera.CFrame
            if (flyCtrl.l+flyCtrl.r)~=0 or (flyCtrl.f+flyCtrl.b)~=0 then
                flyBV.Velocity=(camCF.LookVector*(flyCtrl.f+flyCtrl.b)+((camCF*CFrame.new(flyCtrl.l+flyCtrl.r,(flyCtrl.f+flyCtrl.b)*0.2,0)).Position-camCF.Position))*flySpd
                flyLastCtrl={f=flyCtrl.f,b=flyCtrl.b,l=flyCtrl.l,r=flyCtrl.r}
            elseif flySpd~=0 then
                flyBV.Velocity=(camCF.LookVector*(flyLastCtrl.f+flyLastCtrl.b)+((camCF*CFrame.new(flyLastCtrl.l+flyLastCtrl.r,(flyLastCtrl.f+flyLastCtrl.b)*0.2,0)).Position-camCF.Position))*flySpd
            else flyBV.Velocity=Vector3.new(0,0,0) end
            if UIS:IsKeyDown(Enum.KeyCode.Space) or mobFlyCtrl.u then
                pcall(function() torso.CFrame=torso.CFrame*CFrame.new(0,1,0) end)
            elseif UIS:IsKeyDown(Enum.KeyCode.C) or UIS:IsKeyDown(Enum.KeyCode.LeftControl) or mobFlyCtrl.d then
                pcall(function() torso.CFrame=torso.CFrame*CFrame.new(0,-1,0) end)
            end
            pcall(function() flyBG.CFrame=camCF*CFrame.Angles(-math.rad((flyCtrl.f+flyCtrl.b)*50*flySpd/math.max(GetFlyMax(),1)),0,0) end)
        end
        if flyActive then StopFly() end
    end)
end

if IsMobile then
    local DPAD_SZ=S(160)
    local BTN=S(46)
    local GAP=S(4)

    local DPad=Instance.new("Frame",GUI)
    DPad.Size=UDim2.new(0,DPAD_SZ,0,DPAD_SZ)
    DPad.Position=UDim2.new(1,-DPAD_SZ-S(10),0.5,-DPAD_SZ/2)
    DPad.BackgroundTransparency=1 DPad.ZIndex=50

    local function dBtn(txt,col,x,y,onHold)
        local B=Instance.new("TextButton",DPad)
        B.Text=txt B.Size=UDim2.new(0,BTN,0,BTN) B.Position=UDim2.new(0,x,0,y)
        B.BackgroundColor3=col B.TextColor3=T.White B.Font=Enum.Font.GothamBold B.TextSize=S(14)
        B.BorderSizePixel=0 B.AutoButtonColor=false B.ZIndex=51 uiCorner(B,S(10))
        B.BackgroundTransparency=0.25
        B.InputBegan:Connect(function(i) if i.UserInputType==Enum.UserInputType.Touch then onHold(true) Tw(B,{BackgroundTransparency=0},0.08) end end)
        B.InputEnded:Connect(function(i) if i.UserInputType==Enum.UserInputType.Touch then onHold(false) Tw(B,{BackgroundTransparency=0.25},0.1) end end)
        return B
    end

    local cx=(DPAD_SZ-BTN)/2
    local top=0 local bot=DPAD_SZ-BTN local mid=(DPAD_SZ-BTN)/2
    local left=0 local right=DPAD_SZ-BTN

    dBtn("▲",T.DPad,cx,top,      function(v) mobFlyCtrl.f=v end)
    dBtn("▼",T.DPad,cx,bot,      function(v) mobFlyCtrl.b=v end)
    dBtn("◀",T.DPad,left,mid,    function(v) mobFlyCtrl.l=v end)
    dBtn("▶",T.DPad,right,mid,   function(v) mobFlyCtrl.r=v end)

    local VBAR_W=S(52) local VBAR_H=S(106)
    local VBar=Instance.new("Frame",GUI)
    VBar.Size=UDim2.new(0,VBAR_W,0,VBAR_H)
    VBar.Position=UDim2.new(1,-DPAD_SZ-VBAR_W-S(20),0.5,-VBAR_H/2)
    VBar.BackgroundColor3=T.Panel VBar.BorderSizePixel=0 VBar.ZIndex=50
    uiCorner(VBar,S(12)) uiStroke(VBar,T.Sky,1)
    local VBL=Instance.new("UIListLayout",VBar)
    VBL.FillDirection=Enum.FillDirection.Vertical VBL.HorizontalAlignment=Enum.HorizontalAlignment.Center
    VBL.VerticalAlignment=Enum.VerticalAlignment.Center VBL.Padding=UDim.new(0,S(6))
    uiPad(VBar,S(6),S(6),S(8),S(8))

    local function vBtn(txt,col,onHold)
        local B=Instance.new("TextButton",VBar) B.Text=txt B.Size=UDim2.new(1,0,0,S(40))
        B.BackgroundColor3=col B.TextColor3=T.White B.Font=Enum.Font.GothamBold B.TextSize=S(11)
        B.BorderSizePixel=0 B.AutoButtonColor=false B.ZIndex=51 uiCorner(B,S(8))
        B.BackgroundTransparency=0.2
        B.InputBegan:Connect(function(i) if i.UserInputType==Enum.UserInputType.Touch then onHold(true) Tw(B,{BackgroundTransparency=0},0.08) end end)
        B.InputEnded:Connect(function(i) if i.UserInputType==Enum.UserInputType.Touch then onHold(false) Tw(B,{BackgroundTransparency=0.2},0.1) end end)
    end
    vBtn("▲UP",T.Sky,                      function(v) mobFlyCtrl.u=v end)
    vBtn("▼DN",Color3.fromRGB(50,100,200), function(v) mobFlyCtrl.d=v end)
end

local lastCollect=0
local function FireCollectE()
    pcall(function()
        local info=ContextAction:GetAllBoundActionInfo()
        for name,_ in pairs(info) do
            local nl=name:lower()
            if nl:find("collect") or nl:find("interact") or nl:find("pick") or nl:find("gather") or nl:find("use") or nl:find("grab") then
                pcall(function() ContextAction:CallFunction(name,Enum.UserInputState.Begin,nil) end)
            end
        end
    end)
    if _S.keypress then pcall(_S.keypress,0x45) end
    local myRoot=Root()
    if myRoot then
        for _,v in pairs(workspace:GetDescendants()) do
            if v:IsA("ProximityPrompt") and v.Enabled then
                local part=v.Parent:IsA("BasePart") and v.Parent
                    or (v.Parent:IsA("Model") and (v.Parent.PrimaryPart or v.Parent:FindFirstChildOfClass("BasePart")))
                if part then
                    local dist=(myRoot.Position-part.Position).Magnitude
                    if dist<=v.MaxActivationDistance+5 then
                        if _S.fireprox then pcall(_S.fireprox,v) else pcall(function() v:InputHoldBegin() end) end
                    end
                end
            end
            if v:IsA("ClickDetector") then
                local part=v.Parent
                if part and part:IsA("BasePart") then
                    local dist=(myRoot.Position-part.Position).Magnitude
                    if dist<=v.MaxActivationDistance+5 then
                        if _S.fireclick then pcall(_S.fireclick,v) else pcall(function() v.MouseClick:Fire(LP) end) end
                    end
                end
            end
        end
    end
end
RunService.Heartbeat:Connect(function()
    if not Cfg.FastCollect.On then return end
    local interval=0.05+(Cfg.FastCollect.Rate-1)*(0.95/19)
    if tick()-lastCollect<interval then return end
    lastCollect=tick() pcall(FireCollectE)
end)

local _acConns={}
local _acMeta={}

local function StopAntiAC()
    for _,c in pairs(_acConns) do pcall(function() c:Disconnect() end) end
    _acConns={}
    for obj,mt in pairs(_acMeta) do pcall(function() setmetatable(obj,mt) end) end
    _acMeta={}
end

local function StartAntiAC(h)
    StopAntiAC()
    if not h then return end

    local function lock(prop,getTarget)
        local conn=h:GetPropertyChangedSignal(prop):Connect(function()
            if not Cfg.AntiAC.On then return end
            local target=getTarget()
            if target and h[prop]~=target then
                task.defer(function()
                    for _=1,5 do
                        pcall(function() h[prop]=target end)
                        task.wait()
                    end
                end)
            end
        end)
        table.insert(_acConns,conn)
    end

    lock("WalkSpeed",function() return Cfg.Speed.On and Cfg.Speed.Value or nil end)
    lock("JumpPower", function() return Cfg.InfJump.On and Cfg.InfJump.Power or nil end)
    lock("MaxHealth", function() return nil end)
    lock("Health",    function() return nil end)

    local conn2=h:GetPropertyChangedSignal("WalkSpeed"):Connect(function()
        if not Cfg.AntiAC.On or not Cfg.Speed.On then return end
        if h.WalkSpeed~=Cfg.Speed.Value then
            task.spawn(function()
                for _=1,8 do pcall(function() h.WalkSpeed=Cfg.Speed.Value end) task.wait(0.01) end
            end)
        end
    end)
    table.insert(_acConns,conn2)

    local conn3=h:GetPropertyChangedSignal("JumpPower"):Connect(function()
        if not Cfg.AntiAC.On or not Cfg.InfJump.On then return end
        if h.JumpPower~=Cfg.InfJump.Power then
            task.spawn(function()
                for _=1,8 do pcall(function() h.JumpPower=Cfg.InfJump.Power end) task.wait(0.01) end
            end)
        end
    end)
    table.insert(_acConns,conn3)

    if setmetatable then
        pcall(function()
            local orig=getmetatable(h) or {}
            _acMeta[h]=orig
            local mt={}
            for k,v in pairs(orig) do mt[k]=v end
            mt.__newindex=function(self,key,val)
                if key=="WalkSpeed" and Cfg.AntiAC.On and Cfg.Speed.On then
                    rawset(self,key,Cfg.Speed.Value) return
                end
                if key=="JumpPower" and Cfg.AntiAC.On and Cfg.InfJump.On then
                    rawset(self,key,Cfg.InfJump.Power) return
                end
                rawset(self,key,val)
            end
            setmetatable(h,mt)
        end)
    end

    task.spawn(function()
        while Cfg.AntiAC.On and h and h.Parent do
            if Cfg.Speed.On   and h.WalkSpeed~=Cfg.Speed.Value   then pcall(function() h.WalkSpeed=Cfg.Speed.Value   end) end
            if Cfg.InfJump.On and h.JumpPower~=Cfg.InfJump.Power then pcall(function() h.JumpPower=Cfg.InfJump.Power end) end
            task.wait(0.05)
        end
    end)
end

local TABS={{name="Combat",icon="⚔️"},{name="Move",icon="🏃"},{name="Fly",icon="✈️"},{name="Server",icon="🌐"},{name="Config",icon="💾"}}
local TabBtns={} local ActiveTab=nil

for _,tab in ipairs(TABS) do
    local Btn=Instance.new("TextButton",TabBar)
    Btn.Size=UDim2.new(0.2,-S(3),1,0) Btn.BackgroundColor3=T.Card
    Btn.Text=tab.icon.." "..tab.name Btn.TextSize=S(10)
    Btn.BorderSizePixel=0 Btn.Font=Enum.Font.GothamBold Btn.TextColor3=T.Sub Btn.AutoButtonColor=false
    uiCorner(Btn,S(6)) TabBtns[tab.name]=Btn
end

local function ClearScroll()
    for _,c in pairs(Scroll:GetChildren()) do
        if not c:IsA("UIListLayout") and not c:IsA("UIPadding") then c:Destroy() end
    end
end

local function SwitchTab(name)
    if ActiveTab==name then return end
    if ActiveTab and TabBtns[ActiveTab] then Tw(TabBtns[ActiveTab],{BackgroundColor3=T.Card,TextColor3=T.Sub}) end
    ClearScroll() ActiveTab=name
    Tw(TabBtns[name],{BackgroundColor3=T.Acc,TextColor3=T.White})

    if name=="Combat" then
        Section("Fling",T.Red)
        Toggle("Mini Fling","💥","G = кинуть ближайшего",T.Red,function() return Cfg.Fling.On end,function(v) Cfg.Fling.On=v end,function(v) Notify("💥 Fling",v and "ON" or "OFF",T.Red,2) end)
        Slider("Fling Power","🚀",T.Red,50,800,function() return Cfg.Fling.Power end,function(v) Cfg.Fling.Power=v end," pw")
        Slider("Fling Range","📡",T.Red,5,60,function() return Cfg.Fling.Range end,function(v) Cfg.Fling.Range=v end," st")
        Section("Auto Spam",T.Acc)
        Toggle("Auto Spam","⚡","Авто-стрельба / клик",T.Acc,function() return Cfg.Spam.On end,function(v) Cfg.Spam.On=v end,function(v) Notify("⚡ Spam",v and "ON" or "OFF",T.Acc,2) end)
        Slider("Spam Delay","⏱",T.Acc,1,50,function() return Cfg.Spam.Delay end,function(v) Cfg.Spam.Delay=v end," ms")
        Section("Fast Collect",T.Lime)
        Toggle("Fast Collect E","🌿","Авто-сбор: E + Prompts",T.Lime,function() return Cfg.FastCollect.On end,function(v) Cfg.FastCollect.On=v end,function(v) Notify("🌿 Collect",v and "ON" or "OFF",T.Lime,2) end)
        Slider("Collect Rate","⏩",T.Lime,1,20,function() return Cfg.FastCollect.Rate end,function(v) Cfg.FastCollect.Rate=v end," (1=fast)")

    elseif name=="Move" then
        Section("Speed",T.Blue)
        Toggle("Speed Hack","💨","Кастомная скорость",T.Blue,function() return Cfg.Speed.On end,
            function(v) Cfg.Speed.On=v local h=Hum() if h then h.WalkSpeed=v and Cfg.Speed.Value or 16 end end,
            function(v) Notify("💨 Speed",v and "ON "..Cfg.Speed.Value.."WS" or "OFF",T.Blue,2) end)
        Slider("Speed Value","🔢",T.Blue,16,500,function() return Cfg.Speed.Value end,
            function(v) Cfg.Speed.Value=v if Cfg.Speed.On then local h=Hum() if h then h.WalkSpeed=v end end end," ws")
        Section("Jump",T.Purple)
        Toggle("Infinity Jump","🚀","Прыжок без лимита",T.Purple,function() return Cfg.InfJump.On end,function(v) Cfg.InfJump.On=v end,function(v) Notify("🚀 InfJump",v and "ON" or "OFF",T.Purple,2) end)
        Slider("Jump Power","⬆️",T.Purple,50,400,function() return Cfg.InfJump.Power end,function(v) Cfg.InfJump.Power=v end," jp")
        Section("Защита",T.Cyan)
        Toggle("Shift Lock","🔒","Поворот за камерой",T.Cyan,function() return Cfg.ShiftLock.On end,function(v) Cfg.ShiftLock.On=v end,function(v) Notify("🔒 ShiftLock",v and "ON" or "OFF",T.Cyan,2) end)
        Toggle("Anti Ragdoll","🛡️","Отключает суставы",T.Green,function() return Cfg.AntiRag.On end,function(v) Cfg.AntiRag.On=v end,function(v) Notify("🛡️ AntiRag",v and "ON" or "OFF",T.Green,2) end)
        Toggle("Anti Anti-Cheat","🔮","Metatable + Signal + Loop",T.Cyan,function() return Cfg.AntiAC.On end,
            function(v)
                Cfg.AntiAC.On=v
                if v then local h=Hum() if h then StartAntiAC(h) end
                else StopAntiAC() end
            end,
            function(v) Notify("🔮 AntiAC",v and "Защита активна (3 слоя)" or "OFF",T.Cyan,2) end)

    elseif name=="Fly" then
        Section("✈️ Fly System",T.Sky)
        local IC=Instance.new("Frame",Scroll) IC.Size=UDim2.new(1,0,0,S(IsMobile and 100 or 82))
        IC.BackgroundColor3=T.Card IC.BorderSizePixel=0 uiCorner(IC,S(10)) uiStroke(IC,T.Sky,1)
        local lines=IsMobile and {
            "📱 D-Pad (справа) — вперёд/назад/влево/вправо",
            "▲UP / ▼DN (справа) — высота",
            "🔘 Fly кнопка внизу — вкл/выкл",
        } or {
            "⌨️ WASD        — направление полёта",
            "Space         — подняться вверх",
            "C / LCtrl     — опуститься вниз",
            "F key         — вкл / выкл полёт",
        }
        for i,l in ipairs(lines) do
            local LL=Instance.new("TextLabel",IC) LL.Text=l LL.Size=UDim2.new(1,-S(16),0,S(16)) LL.Position=UDim2.new(0,S(10),0,(i-1)*S(20)+S(8))
            LL.BackgroundTransparency=1 LL.TextColor3=T.Text LL.Font=Enum.Font.Gotham LL.TextSize=S(11) LL.TextXAlignment=Enum.TextXAlignment.Left
        end
        Toggle("Fly","✈️","Полёт R6 + R15",T.Sky,function() return flyActive end,
            function(v) if v and not flyActive then StartFly() elseif not v and flyActive then StopFly() end end,
            function(v) Cfg.Fly.On=v ScheduleSave() Notify("✈️ Fly",v and "Включён!" or "Выключен",T.Sky,2) end)
        Slider("Fly Speed","🏎️",T.Sky,1,10,function() return Cfg.Fly.Speed end,function(v) Cfg.Fly.Speed=v end," x")

    elseif name=="Server" then
        Section("Server Tools",T.Blue)
        ActionBtn("Rejoin","🔄",T.Blue,"Переподключиться",function()
            Notify("🔄 Rejoin","Переподключаемся...",T.Blue,2)
            task.delay(1,function() pcall(function() TeleportService:Teleport(game.PlaceId,LP) end) end)
        end)
        ActionBtn("Server Hop","🌐",T.Purple,"Найти новый сервер",function()
            Notify("🌐 ServerHop","Ищем сервер...",T.Purple,3)
            task.spawn(function()
                local ok2,servers=pcall(function() return HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/"..game.PlaceId.."/servers/Public?sortOrder=Desc&limit=100")) end)
                if ok2 and servers and servers.data then
                    for _,s in ipairs(servers.data) do
                        if s.id~=game.JobId and s.playing<s.maxPlayers then
                            Notify("🌐 Прыгаем!","Сервер найден",T.Purple,2) task.wait(0.5)
                            pcall(function() TeleportService:TeleportToPlaceInstance(game.PlaceId,s.id,LP) end) return
                        end
                    end
                end
                Notify("🌐 Fallback","Перезаход",T.Purple,2) task.wait(0.5)
                pcall(function() TeleportService:Teleport(game.PlaceId,LP) end)
            end)
        end)
        Section("Lag",T.Red)
        Toggle("Server Lag","⚡","Спам RemoteEvents",T.Red,function() return Cfg.ServLag.On end,function(v) Cfg.ServLag.On=v end,function(v) Notify("⚡ ServLag",v and "ЛАГАЕМ!" or "Стоп",T.Red,2) end)
        Slider("Lag Threads","🧵",T.Red,10,200,function() return Cfg.ServLag.Threads end,function(v) Cfg.ServLag.Threads=v end," thr")

    elseif name=="Config" then
        Section("💾 Config",T.Yellow)
        local IC=Instance.new("Frame",Scroll) IC.Size=UDim2.new(1,0,0,S(64))
        IC.BackgroundColor3=T.Card IC.BorderSizePixel=0 uiCorner(IC,S(10)) uiStroke(IC,T.Yellow,1)
        local function cfgStatus()
            if _S.readfile then local ok2,raw=sCall(_S.readfile,CFG_FILE) if ok2 and raw and #raw>2 then return "✅ Найден ("..#raw.." байт)" end end
            return _S.writefile and "⚠️ Не создан — нажми Save" or "❌ ФС не поддерживается"
        end
        local FL=Instance.new("TextLabel",IC) FL.Text="📂 "..CFG_FILE FL.Size=UDim2.new(1,-S(16),0,S(18)) FL.Position=UDim2.new(0,S(10),0,S(6))
        FL.BackgroundTransparency=1 FL.TextColor3=T.Yellow FL.Font=Enum.Font.GothamBold FL.TextSize=S(11) FL.TextXAlignment=Enum.TextXAlignment.Left
        local SL=Instance.new("TextLabel",IC) SL.Text=cfgStatus() SL.Size=UDim2.new(1,-S(16),0,S(14)) SL.Position=UDim2.new(0,S(10),0,S(27))
        SL.BackgroundTransparency=1 SL.TextColor3=T.Sub SL.Font=Enum.Font.Gotham SL.TextSize=S(10) SL.TextXAlignment=Enum.TextXAlignment.Left
        local AL=Instance.new("TextLabel",IC) AL.Text="⚡ Авто-сохранение: 1.5с"
        AL.Size=UDim2.new(1,-S(16),0,S(13)) AL.Position=UDim2.new(0,S(10),0,S(46))
        AL.BackgroundTransparency=1 AL.TextColor3=T.Sub AL.Font=Enum.Font.Gotham AL.TextSize=S(9) AL.TextXAlignment=Enum.TextXAlignment.Left
        ActionBtn("Save Now","💾",T.Yellow,"Принудительное сохранение",function() SaveCfg(Cfg) SL.Text=cfgStatus() Notify("💾 Сохранено","CFG обновлён",T.Yellow,2) end)
        ActionBtn("Reset Defaults","🔄",T.Red,"Сброс всех настроек",function()
            Cfg=DeepCopy(DEFAULT) SaveCfg(Cfg) SL.Text=cfgStatus() if flyActive then StopFly() end Notify("🔄 Сброс","По умолчанию",T.Red,2)
        end)
        Section("Экран",T.Cyan)
        local SIC=Instance.new("Frame",Scroll) SIC.Size=UDim2.new(1,0,0,S(44))
        SIC.BackgroundColor3=T.Card SIC.BorderSizePixel=0 uiCorner(SIC,S(10)) uiStroke(SIC,T.Cyan,1)
        local SIL=Instance.new("TextLabel",SIC)
        SIL.Text=string.format("📐 %dx%d  ·  Scale: %.2f  ·  %s",math.floor(SW),math.floor(SH),Scale,IsMobile and "Mobile" or "PC")
        SIL.Size=UDim2.new(1,-S(16),0,S(18)) SIL.Position=UDim2.new(0,S(10),0,S(7))
        SIL.BackgroundTransparency=1 SIL.TextColor3=T.Cyan SIL.Font=Enum.Font.GothamBold SIL.TextSize=S(11) SIL.TextXAlignment=Enum.TextXAlignment.Left
        local SIL2=Instance.new("TextLabel",SIC) SIL2.Text="Beta 0.0.1  ·  Executor safe  ·  Anti-AC: 3 layers"
        SIL2.Size=UDim2.new(1,-S(16),0,S(13)) SIL2.Position=UDim2.new(0,S(10),0,S(26))
        SIL2.BackgroundTransparency=1 SIL2.TextColor3=T.Sub SIL2.Font=Enum.Font.Gotham SIL2.TextSize=S(9) SIL2.TextXAlignment=Enum.TextXAlignment.Left
    end
end

SwitchTab("Combat")
for _,tab in ipairs(TABS) do
    TabBtns[tab.name].MouseButton1Click:Connect(function() SwitchTab(tab.name) end)
    TabBtns[tab.name].TouchTap:Connect(function() SwitchTab(tab.name) end)
end

local minimized=false
MinBtn.MouseButton1Click:Connect(function()
    minimized=not minimized
    Tw(Win,{Size=minimized and UDim2.new(0,WIN_W,0,S(57)) or UDim2.new(0,WIN_W,0,WIN_H)},0.3,Enum.EasingStyle.Back)
    TabBar.Visible=not minimized Scroll.Visible=not minimized MinBtn.Text=minimized and "+" or "−"
end)
local dragStart,winStart
Head.InputBegan:Connect(function(i)
    if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then
        dragStart=i.Position winStart=Win.Position
    end
end)
UIS.InputChanged:Connect(function(i)
    if dragStart and (i.UserInputType==Enum.UserInputType.MouseMovement or i.UserInputType==Enum.UserInputType.Touch) then
        local d=i.Position-dragStart
        Win.Position=UDim2.new(winStart.X.Scale,winStart.X.Offset+d.X,winStart.Y.Scale,winStart.Y.Offset+d.Y)
    end
end)
UIS.InputEnded:Connect(function(i)
    if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then dragStart=nil end
end)

if IsMobile then
    local BAR_W=math.min(S(340),SW-S(20))
    local BBar=Instance.new("Frame",GUI)
    BBar.Size=UDim2.new(0,BAR_W,0,S(54)) BBar.Position=UDim2.new(0.5,-BAR_W/2,1,-S(72))
    BBar.BackgroundColor3=T.Panel BBar.BorderSizePixel=0 uiCorner(BBar,S(14)) uiStroke(BBar,T.Acc,1)
    local BBL=Instance.new("UIListLayout",BBar) BBL.FillDirection=Enum.FillDirection.Horizontal
    BBL.HorizontalAlignment=Enum.HorizontalAlignment.Center BBL.VerticalAlignment=Enum.VerticalAlignment.Center BBL.Padding=UDim.new(0,S(4))
    uiPad(BBar,S(5),S(5),S(7),S(7))
    local BTN_W=math.floor((BAR_W-S(50))/6)
    local function MobBtn(txt,col,cb)
        local B=Instance.new("TextButton",BBar) B.Size=UDim2.new(0,BTN_W,0,S(38))
        B.BackgroundColor3=col B.Text=txt B.TextSize=S(9) B.Font=Enum.Font.GothamBold
        B.TextColor3=T.White B.BorderSizePixel=0 B.AutoButtonColor=false uiCorner(B,S(9))
        local function act() Tw(B,{BackgroundColor3=T.White},0.08) task.delay(0.15,function() Tw(B,{BackgroundColor3=col},0.15) end) pcall(cb) end
        B.TouchTap:Connect(act) B.MouseButton1Click:Connect(act) return B
    end
    MobBtn("✈️Fly", T.Sky,    function() if not flyActive then StartFly() else StopFly() end Notify("✈️",flyActive and "ON" or "OFF",T.Sky,1.5) end)
    MobBtn("💨Spd", T.Blue,   function() Cfg.Speed.On=not Cfg.Speed.On ScheduleSave() local h=Hum() if h then h.WalkSpeed=Cfg.Speed.On and Cfg.Speed.Value or 16 end Notify("💨",Cfg.Speed.On and "ON" or "OFF",T.Blue,1.5) end)
    MobBtn("🚀Jmp", T.Purple, function() Cfg.InfJump.On=not Cfg.InfJump.On ScheduleSave() Notify("🚀",Cfg.InfJump.On and "ON" or "OFF",T.Purple,1.5) end)
    MobBtn("🌿Clc", T.Lime,   function() Cfg.FastCollect.On=not Cfg.FastCollect.On ScheduleSave() Notify("🌿",Cfg.FastCollect.On and "ON" or "OFF",T.Lime,1.5) end)
    MobBtn("💥Flg", T.Red,    function()
        if not Cfg.Fling.On then Notify("💥","Включи в Combat!",T.Red,2) return end
        local root=Root() if not root then return end
        local near,best=nil,math.huge
        for _,p in pairs(Players:GetPlayers()) do if p~=LP and p.Character then
            local pr=p.Character:FindFirstChild("HumanoidRootPart") if pr then
                local dd=(root.Position-pr.Position).Magnitude if dd<best and dd<Cfg.Fling.Range then best=dd near=pr end
            end
        end end
        if near then local dir=(near.Position-root.Position).Unit pcall(function() near.Velocity=dir*Cfg.Fling.Power+Vector3.new(math.random(-50,50),Cfg.Fling.Power*0.6,math.random(-50,50)) end) end
    end)
    MobBtn("🌐Hop", T.Purple, function() Notify("🌐","Переход...",T.Purple,2) task.delay(1,function() pcall(function() TeleportService:Teleport(game.PlaceId,LP) end) end) end)
end

UIS.InputBegan:Connect(function(input,gpe)
    if gpe then return end
    if input.KeyCode==Enum.KeyCode.F then
        if not flyActive then StartFly() else StopFly() end Notify("✈️ Fly",flyActive and "Включён!" or "Выключен",T.Sky,2)
    end
    if input.KeyCode==Enum.KeyCode.G and Cfg.Fling.On then
        local root=Root() if not root then return end
        local near,best=nil,math.huge
        for _,p in pairs(Players:GetPlayers()) do if p~=LP and p.Character then
            local pr=p.Character:FindFirstChild("HumanoidRootPart") if pr then
                local dd=(root.Position-pr.Position).Magnitude if dd<best and dd<Cfg.Fling.Range then best=dd near=pr end
            end
        end end
        if near then
            local dir=(near.Position-root.Position).Unit
            pcall(function() near.Velocity=dir*Cfg.Fling.Power+Vector3.new(math.random(-60,60),Cfg.Fling.Power*0.65,math.random(-60,60)) end)
            Notify("💥 Fling!",near.Parent.Name,T.Red,1.5)
        end
    end
end)

local function ApplyAll(char,isRespawn)
    task.wait(0.7) local h=char and char:FindFirstChildOfClass("Humanoid") if not h then return end
    if Cfg.Speed.On   then pcall(function() h.WalkSpeed=Cfg.Speed.Value end) end
    if Cfg.InfJump.On then pcall(function() h.JumpPower=Cfg.InfJump.Power end) end
    if Cfg.AntiRag.On then for _,v in pairs(char:GetDescendants()) do
        if v:IsA("BallSocketConstraint") or v:IsA("HingeConstraint") then pcall(function() v.Enabled=false end) end
    end end
    if Cfg.AntiAC.On then StartAntiAC(h) end
    if flyActive then StopFly() end
    if isRespawn then
        local feats={}
        if Cfg.Speed.On then table.insert(feats,"💨"..Cfg.Speed.Value) end
        if Cfg.InfJump.On then table.insert(feats,"🚀") end
        if Cfg.AntiRag.On then table.insert(feats,"🛡️") end
        if Cfg.AntiAC.On then table.insert(feats,"🔮") end
        if Cfg.FastCollect.On then table.insert(feats,"🌿") end
        if #feats>0 then Notify("🔄 Reload",table.concat(feats," "),T.Blue,3) end
    end
end
LP.CharacterAdded:Connect(function(char) StopAntiAC() ApplyAll(char,true) end)
if LP.Character then ApplyAll(LP.Character,false) end

RunService.Heartbeat:Connect(function()
    if _saveT>0 and tick()-_saveT>1.5 then _saveT=0 SaveCfg(Cfg) end
    if Cfg.Speed.On then local h=Hum() if h and h.WalkSpeed~=Cfg.Speed.Value then pcall(function() h.WalkSpeed=Cfg.Speed.Value end) end end
    if Cfg.AntiRag.On then local c=Char() if c then for _,v in pairs(c:GetDescendants()) do
        if v:IsA("BallSocketConstraint") or v:IsA("HingeConstraint") then pcall(function() v.Enabled=false end) end
    end end end
end)

UIS.JumpRequest:Connect(function()
    if not Cfg.InfJump.On then return end
    local h=Hum() if not h then return end
    pcall(function() h:ChangeState(Enum.HumanoidStateType.Jumping) end)
    local root=Root() if root then
        local bv=Instance.new("BodyVelocity") bv.Velocity=Vector3.new(root.Velocity.X,Cfg.InfJump.Power,root.Velocity.Z)
        bv.MaxForce=Vector3.new(0,4e5,0) bv.P=1e5 bv.Parent=root Debris:AddItem(bv,0.12)
    end
end)

RunService.RenderStepped:Connect(function()
    local char=Char() if not char then return end
    local root=char:FindFirstChild("HumanoidRootPart") local h=char:FindFirstChildOfClass("Humanoid")
    if not (root and h) then return end
    if Cfg.ShiftLock.On then
        UIS.MouseBehavior=Enum.MouseBehavior.LockCenter
        local lv=Camera.CFrame.LookVector local xz=Vector3.new(lv.X,0,lv.Z)
        if xz.Magnitude>0.01 then pcall(function() root.CFrame=root.CFrame:Lerp(CFrame.new(root.CFrame.Position,root.CFrame.Position+xz),0.6) end) end
        h.CameraOffset=h.CameraOffset:Lerp(Vector3.new(1.75,0,0),0.2)
    else
        if UIS.MouseBehavior==Enum.MouseBehavior.LockCenter then UIS.MouseBehavior=Enum.MouseBehavior.Default end
        h.CameraOffset=h.CameraOffset:Lerp(Vector3.new(0,0,0),0.2)
    end
end)

local lastSpam=0
RunService.Heartbeat:Connect(function()
    if not Cfg.Spam.On then return end
    if tick()-lastSpam<Cfg.Spam.Delay/100 then return end
    lastSpam=tick()
    local char=Char() if not char then return end
    local tool=char:FindFirstChildOfClass("Tool")
    if tool then for _,v in pairs(tool:GetDescendants()) do if v:IsA("RemoteEvent") then pcall(function() v:FireServer(Mouse.Hit) end) end end end
    if _S.mouse1click then pcall(_S.mouse1click) end
end)

local lagRunning=false
RunService.Heartbeat:Connect(function()
    if not Cfg.ServLag.On then lagRunning=false return end
    if lagRunning then return end lagRunning=true
    task.spawn(function()
        while Cfg.ServLag.On do
            for _=1,Cfg.ServLag.Threads do task.spawn(function()
                for _,v in pairs(game:GetDescendants()) do if v:IsA("RemoteEvent") and Cfg.ServLag.On then pcall(function() v:FireServer() end) end end
            end) end task.wait(0.05)
        end lagRunning=false
    end)
end)

task.delay(2.8,function()
    Notify("🥟 Pelmen PVP","Beta 0.0.1 · "..(IsMobile and "📱" or "🖥️").." · Fly D-Pad · AntiAC x3",T.Acc,5)
end)
task.delay(4,function()
    local on={}
    if Cfg.Speed.On then table.insert(on,"💨"..Cfg.Speed.Value) end
    if Cfg.InfJump.On then table.insert(on,"🚀") end
    if Cfg.FastCollect.On then table.insert(on,"🌿") end
    if Cfg.Spam.On then table.insert(on,"⚡") end
    if Cfg.AntiRag.On then table.insert(on,"🛡️") end
    if Cfg.AntiAC.On then table.insert(on,"🔮") end
    if #on>0 then Notify("📂 CFG Восстановлен",table.concat(on," "),T.Yellow,5) end
end)

print("╔═══════════════════════════════════════════════╗")
print("║  🥟 PELMEN PVP  Beta 0.0.1                   ║")
print("║  📐 "..math.floor(SW).."x"..math.floor(SH).."  Scale:"..string.format("%.2f",Scale).."  Mobile:"..(IsMobile and "YES" or "NO ").."      ║")
print("║  🔮 AntiAC: Signal + Metatable + Loop        ║")
print("║  ✈️  Fly D-Pad: ▲▼◀▶ + UP/DN               ║")
print("╚═══════════════════════════════════════════════╝")
